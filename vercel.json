{
  "buildCommand": "cd frontend && npm install ajv@8 --legacy-peer-deps && npm install --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true npm run build",
  "outputDirectory": "frontend/build",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/frontend/build/index.html" }
  ],
  "env": {
    "PYTHONPATH": "api"
  }
}
