
// CaseProgress Component - Visual timeline tracker for workflow steps

interface ProgressStep {
  key: string;
  label: string;
}

interface CaseProgressProps {
  currentStep: string;
  stepsCompleted: string[];
  steps: ProgressStep[];
  onStepClick?: (step: ProgressStep) => void;
}

export default function CaseProgress({
  currentStep,
  stepsCompleted,
  steps,
  onStepClick,
}: CaseProgressProps) {
  const isCompleted = (stepKey: string) => stepsCompleted.includes(stepKey);
  const isActive = (stepKey: string) => stepKey === currentStep;
  const isClickable = (stepKey: string) => stepsCompleted.includes(stepKey) || stepKey === currentStep;

  return (
    <div className="bg-white rounded-lg shadow-sm border border-gray-200 p-4 md:p-6">
      <h3 className="text-sm font-semibold text-gray-500 uppercase tracking-wider mb-4">
        Workflow Progress
      </h3>
      
      <div className="relative">
        {/* Progress Line */}
        <div className="absolute top-6 left-6 md:left-8 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true">
          <div 
            className="h-full bg-gradient-to-b from-blue-500 to-green-500 transition-all duration-500"
            style={{
              height: `${(stepsCompleted.length / steps.length) * 100}%`,
            }}
          />
        </div>
        
        <div className="relative space-y-8">
          {steps.map((step, index) => {
            const completed = isCompleted(step.key);
            const active = isActive(step.key);
            const clickable = onStepClick && isClickable(step.key);
            
            return (
              <div key={step.key} className="relative flex items-start">
                <div className="flex items-center">
                  <button
                    onClick={() => clickable && onStepClick(step)}
                    className={`relative flex items-center justify-center w-12 h-12 rounded-full border-2 transition-all cursor-pointer ${
                      completed
                        ? 'bg-green-500 border-green-500 text-white hover:bg-green-600'
                        : active
                        ? 'bg-blue-500 border-blue-500 text-white hover:bg-blue-600'
                        : 'bg-white border-gray-300 text-gray-400 hover:border-gray-400'\n                    } ${clickable ? 'cursor-pointer transform hover:scale-110' : 'cursor-default'}`}
                    aria-label={`Step ${index + 1}: ${step.label}`}
                    disabled={!clickable}
                  >
                    {completed ? (
                      <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={3} d="M5 13l4 4L19 7" />
                      </svg>
                    ) : (
                      <span className="text-sm font-medium">{index + 1}</span>
                    )}
                  </button>
                </div>
                
                <div className="ml-4 md:ml-8 flex-1 min-w-0 pb-2">
                  <div className="flex items-center justify-between">
                    <div>
                      <h4 className={`text-sm font-medium ${
                        active ? 'text-blue-600' : completed ? 'text-green-600' : 'text-gray-900'
                      }`}>
                        {step.label}
                      </h4>
                      {active && (
                        <p className="text-xs text-blue-500 mt-0.5">
                          In Progress
                          <span className="ml-2 inline-flex h-2 w-2 bg-blue-400 rounded-full animate-pulse"></span>
                        </p>
                      )}
                      {completed && index < steps.length - 1 && (
                        <p className="text-xs text-green-500 mt-0.5">
                          Completed
                        </p>
                      )}
                    </div>
                    {clickable && !completed && (
                      <button
                        onClick={() => onStepClick(step)}
                        className="hidden sm:flex items-center px-2 py-1 text-xs font-medium text-blue-600 bg-blue-50 rounded-lg hover:bg-blue-100 transition-colors"
                      >
                        Resume
                      </button>
                    )}
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>
      
      {/* Progress Stats */}
      <div className="mt-6 pt-4 border-t border-gray-100">
        <div className="flex items-center justify-between text-sm">
          <span className="text-gray-500">Overall Progress</span>
          <span className="font-semibold text-blue-600">
            {stepsCompleted.length} / {steps.length} steps
          </span>
        </div>
        <div className="mt-2 w-full bg-gray-200 rounded-full h-2">
          <div
            className="h-2 rounded-full bg-gradient-to-r from-blue-500 to-green-500 transition-all duration-500"
            style={{
              width: `${(stepsCompleted.length / steps.length) * 100}%`,
            }}
          />
        </div>
      </div>
    </div>
  );
}
