{
  "name": "goisure",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install ajv@8 --legacy-peer-deps && npm install --legacy-peer-deps && DISABLE_ESLINT_PLUGIN=true npm run build",
    "start": "cd api && uvicorn index:app --host 0.0.0.0 --port $PORT"
  }
}